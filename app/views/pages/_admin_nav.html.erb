<% if user_signed_in? %>
  <div id="header">
    <% if current_page?(posts_path) %>
      <p>All Posts</p>
    <% elsif current_page?(posts_path) %>
      <p>About Me</p>
    <% else %>
      <%= link_to "Back to All Posts", posts_path %>
    <% end %>

    <div class="buttons">
        <button class="button"><%= link_to "New Project", new_project_path %></button>
        <button class="button"><%= link_to "New Blog Post", new_post_path %></button>
        <button class="button"><%= link_to "Logout", destroy_user_session_path, :method => :delete %></button>
    </div>
  </div>
<% end %>